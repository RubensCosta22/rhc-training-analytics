# SQL

Consultas SQL utilizadas para extração, exploração e validação dos dados de origem.

Arquivos planejados:

- `source_queries.sql`: consultas de extração e exploração do Supabase/PostgreSQL.
- `validation_queries.sql`: consultas de reconciliação e validação entre origem e pipeline.
