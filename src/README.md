# Source

Código Python reutilizável extraído dos notebooks à medida que o pipeline evoluir.

Estrutura planejada:

```text
src/
├── extract/     # conexão e extração da origem
├── transform/   # limpeza e regras de transformação
└── utils/       # funções auxiliares e validações compartilhadas
```
